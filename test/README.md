# Telefrek Core Testing

## Configuration

[This](https://xunit.net/docs/configuration-files) is a link to the current XUnit configuation setup that was used to setup the xunit.runner.json files in each project.